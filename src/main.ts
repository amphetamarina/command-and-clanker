const app = document.getElementById("app");
if (app) {
  app.textContent = "tty-space-station: skeleton up. v0 step 1 of 9.";
  app.style.color = "#e0e0f0";
  app.style.fontFamily = "monospace";
  app.style.padding = "1rem";
}
